import React from "react";
import UserData from "../UserData";

function Login(props:{isLogin:boolean, onLoginToggle:(()=>any), onLogin:(user:UserData)=>any}) {
    return <div />
}

export default Login;